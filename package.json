{
  "name": "facebook-scraper",
  "version": "1.0.0",
  "description": "Facebook Page Scraper with API endpoint for n8n integration using Puppeteer (puppeteer-extra + stealth).",
  "type": "module",
  "scripts": {
    "start": "node src/scraper.js",
    "start:api": "node src/api-server.js",
    "dev": "node --watch src/api-server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "puppeteer": "^22.15.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
